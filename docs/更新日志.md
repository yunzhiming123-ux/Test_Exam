# 更新日志

## [2026-06-16] MkDocs Quiz 考试系统 + 4 份题库升级

### 变更
- 集成 **mkdocs-quiz** 插件，4 个考试专题文件全部转为交互式 `<quiz>` 格式
- mkdocs.yml 新增 mkdocs_quiz 插件配置（自动编号 + 进度跟踪 + 中文）
- 导航栏模拟考试子菜单改为指向实际文件名

### 交互功能
- 选择题 → 单选/多选交互，点击即提交自动判对错
- 填空题 → 输入框填写 → 提交后逐空比对
- 计算题/代码分析 → 填空输入关键数值/答案 + 隐藏完整解析
- 进度跟踪：侧边栏显示答题进度和正确率

## [2026-06-16] 新增 4 份计算/代码专题题库

### 新增文件
- **13_计算专题.md**（415行）：纯数值计算题，9 道大题
  - DNN 前向传播、Softmax、反向传播误差 & 权重更新
  - 参数量计算、特征图尺寸、交叉熵、Epoch/Batch/Iteration
  - 链式法则与梯度消失证明
- **14_代码分析.md**（366行）：代码分析题，6 道大题
  - 模型结构分析、Embedding+LSTM、批次维度、错误识别
  - 完整模型分析（PoetryModel）、CNN 代码分析
- **15_选择填空.md**（474行）：选择题 12 题 + 填空题 12 题
  - 神经网络/激活函数/损失函数/优化器/CNN/RNN/Transformer
  - 公式填空、对比表格、梯度消失证明填空
- **16_代码编写.md**（703行）：手写代码题，7 道大题
  - 手动实现 Softmax、交叉熵、DNN 前向传播、Linear 反向传播
  - 完整训练循环、Causal Mask、梯度累积训练

### 全部源自
- `docs/机器学习测验答题记录.md` 中的 7 类计算题型
- 每题使用全新数值/代码，不重复原题

## [2026-06-11] MyRdkX5 项目重构：端侧 AI 遥操作 + 动作预测系统

### 变更
- **MyRdkX5.md**：从 Vlog 跟拍机器人重新定位为"端侧 AI 遥操作与动作预测系统"
  - 核心方向：低成本（< ¥1000）端侧动作捕捉 + MOSS 模型预测 + 远程操控
  - 架构创新：Skill + Protocol 双解耦，Skill 极轻量只做协议拼装
  - 算力分配：端侧 10 TOPS 做实时姿态和预测，云侧做语义理解
  - 应用场景：低成本虚拟主播、远程手势控制、远程协作标定
  - 端云协同：实时在端、认知在云，不浪费算力也不硬扛

### 新增
- **docs/HTML展示提示词.md**：用于生成向老师展示的 HTML 演示文档的 AI 提示词

## v2.2 (2026-05-23)

### 修复
- 主题下拉框选择后不实时刷新 → `_on_theme_change` 调用错误方法名
- 学习模式缺 MathJax/highlight.js CDN → 公式和代码高亮不显示

### 变更
- 学习模式改为直接渲染原始 Markdown（不再解析题目卡片），仅练习/考试模式提取题目
- 更适合复习阅读：不会被复习要点误识别为题目打断阅读流

## v2.1 (2026-05-23)

### 新增
- 主题系统：亮色 / 暗色 / 护眼 三套主题
- 自定义颜色：左侧面板"自定义背景""自定义文字"按钮 → 取色器
- 重置按钮：恢复当前主题默认颜色
- 全主题 CSS 动画：卡片淡入、选项涟漪、对勾弹跳、错误抖动、光晕脉冲

### 修复
- **Bug**：题目序号重复导致答案按钮失灵 → QID 改为全局唯一计数器
- **Bug**：section 间 Q1 重复导致 DOM id 冲突 → 每题分配唯一内部ID，保留原始题号显示

## v2.0 (2026-05-23)

### 新增
- 练习模式 & 考试模式（原只有学习模式）
- 选择题交互：点击选项 → 提交 → 自动判对错
- 填空题交互：输入框填写 → 提交 → 逐空比对
- 顶部计分栏（练习模式） / 计时栏（考试模式）
- 搜索框（学习模式下过滤题目）
- 键盘快捷键 `N` 跳下一题
- CSS 动画：卡片淡入、对错反馈弹跳、按钮涟漪

### 修复
- **Bug**：同行多个选项（`A. xxx  B. yyy`）只能识别第一个 → 已修复，现在正确拆分为 4 个独立选项
- **Bug**：选择题答案比对取错字母（从 `**A4.** B` 开头取 `A` 而非 `B`） → 已修复，智能提取正确选项字母
- **Bug**：非选择题的答案也误匹配了字母 → 已修复，仅选择题使用 `answer_letter`

### 变更
- 文件 08 补充了 72 道题的 `<details>` 答案块（从底部答案区提取）

## v1.0 (2026-05-21)

- 初始版本
- 12 份 .md 题库文件，总计 302 道题
- Markdown 阅读器（学习模式 + 折叠答案）
- 支持 MathJax 公式渲染 + highlight.js 代码高亮

---

## [2026-06-09 19:01] 大规模更新：知识全景文档 + 随机考试系统

### 新增文件
- **深度学习知识全景.md** (688行)
  - 不按教材章节，按知识发展脉络组织
  - 覆盖9大主题：神经网络基础、CNN、RNN/LSTM、Transformer、训练核心、优化器、损失函数、对比学习与蒸馏、代码分析
  - 每个知识点都有"问题→方案→新问题→新方案"的发展链
  - 结合PyTorch代码实战，附详细注释
  - 每章末尾3-5道随堂练习（含折叠答案）

- **深度学习随机考试.html** (考试主页面)
- **question_bank.js** (300道题，63KB)
- **exam_logic.js** (考试逻辑)

### 题库覆盖
300道题，12大分类：
- 神经网络基础 (30题)
- CNN (40题)
- RNN/LSTM (30题)
- Transformer (40题)
- 损失函数 (20题)
- 优化器 (15题)
- 正则化 (20题)
- 梯度问题 (15题)
- 代码分析 (30题)
- 对比学习与蒸馏 (15题)
- 激活函数 (15题)
- 多模态 (10题)
- 综合与进阶 (20题)

### 功能特性
- 随机抽题：按分类均匀抽样，保证全面覆盖
- 全量模式：一次性练习全部300题
- 自定义题数：20/30/40/50题
- 倒计时提醒
- 实时进度条
- 详细解析：提交后显示每题正确答案和解释
- 响应式设计：手机/平板/电脑均可使用
